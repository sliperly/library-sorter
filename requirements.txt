# LLM (llm.py uses native ollama SDK, not langchain)
ollama>=0.3.0
pydantic>=2.0.0

# Text extraction
python-docx>=1.1.0

# OCR fallback for scanned PDF/DjVu
easyocr>=1.7.0
pdf2image>=1.16.0
Pillow>=10.0.0

# API requests (Open Library)
requests>=2.31.0

# Config (.env)
python-dotenv>=1.0.0
